{
  "name": "iris-revit-mcp-server",
  "version": "0.1.0",
  "private": true,
  "description": "Local MCP stdio server for the IRIS Revit MCP proof of concept.",
  "type": "module",
  "bin": {
    "iris-revit-mcp-server": "./server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js"
  }
}
